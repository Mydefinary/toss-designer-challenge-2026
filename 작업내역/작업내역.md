# 작업내역

토스 프로덕트 디자이너 챌린지 2026 — "6명이 회의 일정을 잡는 경험 설계" 과제

---

## 2026-06-27 ~ 06-28 · 기획/PRD 수립

### 프로젝트 셋업
- `git init`으로 저장소 생성, `.gitignore` 추가(node/vite/env)
- 메모리 저장: 코딩 위임 정책(opus/sonnet + git worktree 병렬), 한글 응답, 프로젝트 개요

### 과제 분석
- 토스 디자이너 챌린지 2026 페이지 분석 → 주제·미션·평가축(문제정의·솔루션설계·시각완성도)·마감(2026-07-12 23:59) 정리
- 산출물 형태: **반응형 웹 인터랙티브 프로토타입**(GitHub Pages) + 3가지 질문 답변

### PRD.md 작성 및 고도화 (누적)
1. **초안** — 문제정의·솔루션·화면·기술스택·작업분해·마일스톤 기본 골격
2. **4대 변수 도메인 모델 반영** — ① 참석 중요도(필수/선택) ② 참석 가능 3단계(불가·회피·가능) ③ 회의 장소 유무 ④ 사후 이슈 대비
   - **1~5순위 예비 시간(backup ranking)** 컨셉을 핵심 차별점으로 도입 — 이슈 발생 시 다음 순위로 즉시 전환
   - 플랫폼 **웹** 확정
3. **"투명성이 양보를 만든다" 설계 철학 격상** — 정보 비대칭 제거가 자발적 양보를 유도
   - 투명성 보드(6명 전원 상황 히트맵), 슬롯 드릴다운(특정 시간이 왜 부적합한지) 추가
4. **핵심 통증 보강** — "결정 근거의 부재"를 가장 본질적 통증으로 추가(통증 6가지)
5. **데모 시나리오 5종(부록 A)** — 전형적 충돌/완벽한 시간 없음/필수자 충돌/장소 병목/이슈 대응. 각기 다른 엣지케이스 검증
6. **토스 공식 디자인 토큰(5.1)** — Toss Blue `#0064FF`, Toss Gray `#202632` + 확장 팔레트·타이포(Pretendard)·radius/shadow (출처: brand.toss.im)
7. **불가 사유 확장** — `회의`(선약) 태그 추가 + `기타`(직접 입력) 자유 텍스트 사유 기능

### 커밋 이력
- `52cf83a` 과제 분석 및 PRD 작성
- `cb5abd7` 4대 변수 반영, 1~5순위 백업, 웹 확정
- `b04ffa2` 투명성 철학·투명성 보드·슬롯 드릴다운
- `b7776ad` '결정 근거의 부재' 통증 추가
- `126061b` 데모 시나리오 5종 + 토스 디자인 토큰
- `ca4c2e7` 불가 사유 '회의' + 직접 입력(기타)

---

## 2026-06-28 · 구현 1차 (트랙 A+B)

### PRD 빈틈 보강(추가)
- 과제 원문·3대 질문 정확 문구 반영, "다음 주까지" 시급성 추가
- 점수모델 구체화(가중치·tie-break·순위 다양성), 제약완화 제안 **풀버전**(4종 완화·gain/cost 정렬·2단계 조합)
- 응답수집 스코프·반응형 투명성 보드·비범위(10장) 명시

### 트랙 A+B 구현 (opus 서브에이전트 위임)
- **worktree 격리 실패**(한글 경로 추정) → 단일 에이전트 비격리 백그라운드로 진행, 별도 브랜치 커밋 후 메인 병합
- 브랜치 `track-ab-scaffold-algo` (3 커밋) → master 병합 `c867640`
- 스캐폴딩: Vite+React+TS(strict), HashRouter, 디자인 토큰(tokens.css, Pretendard), 공용 UI 프리미티브(Button/Chip/Card/Badge/Avatar/AvatarStack), 화면 5종 stub
- 도메인: `src/types.ts`, `src/lib/recommend.ts`(슬롯생성·점수·tie-break·다양성·`suggestRelaxations` 풀버전), `src/data/scenarios.ts`(5종 시드)
- 품질 게이트 전부 PASS: tsc strict 0 errors / build OK / **테스트 9/9**

### 다음 단계
- 2차-①: 앱 셸 + 전역 스토어(Zustand) + 라우팅 흐름 + 시나리오 셀렉터 (opus 선행)
- 2차-②: 셸 병합 후 화면 트랙 C(생성/제약입력)·D(추천/확정)·E(운영+문서·배포) 병렬

---

## 2026-06-28 · 구현 2차 (트랙 track-shell-store)

### Zustand v5 전역 스토어 도입

- `npm i zustand` 설치(v5)
- `src/store/meetingStore.ts` 신설 — 단일 소스 상태(scenarioId/config/attendees/constraints)에서 `recommend()`·`suggestRelaxations()` 파생값(candidates/relaxations) 산출
- 확정 흐름: confirmedRanking·currentRankIndex 관리, 이슈 로그(issueLog), 완화 undo 스택(appliedRelaxations)
- 액션 9종: loadScenario / setConfig / setAttendeeRole / setConstraint / recompute / confirm / moveToNextRank / applyRelaxation / undoRelaxation
- 순수 헬퍼 `buildScenarioState` / `deriveCandidates` 로 초기화·액션 공유, `structuredClone` 깊은복사
- 셀렉터 훅 10종 + `useScenarioMeta` / `useMeetingActions`(`useShallow` 안정화)
- 배럴: `src/store/index.ts`

### 모바일 셸 구현

- `src/components/AppShell.tsx` + `AppShell.module.css` 신설
  - 최대폭 420px 고정, 자체 워드마크 **MEETSYNC**
  - 시나리오 셀렉터(`loadScenario` 연동)
  - 1~5 단계 인디케이터(`useLocation` 기반)
- `src/App.tsx` 내 Routes 전체를 AppShell로 래핑

### 화면 스텁 스토어 배선

- 5개 화면 스텁에 스토어 값 최소 배선 완료 (본문 UI는 트랙 C/D/E 담당)
- `global.css`에 `.screen-stub` / `.stub-meta` 스타일 추가

### 품질 게이트

- tsc 0 errors, vitest 9/9, vite build 성공

### 커밋 이력

- `d4adc09` zustand 스토어(meetingStore + 배럴)
- `9b9dbc3` 모바일 셸·라우팅·화면 배선·스타일

---

## 2026-06-28 · 구현 3차 (화면 트랙 C·D·E 병렬)

### 병렬 디스패치 (opus ×3, 백그라운드)
스토어 계약 확정 후 3개 화면 트랙을 동시 실행. 각 트랙은 자기 화면 파일 + co-located 폴더만 수정, 공통계약/`package.json` 무수정.
- **트랙 C** — 화면1(회의 생성: 제목·장소토글·필수/선택), 화면2(제약 입력: 참석자별 요일×시간 3단계 토글·불가 사유 5종+기타 직접입력). `screens/create/`, `screens/constraints/`
- **트랙 D** — 화면3(추천 결과: 반응형 투명성 보드·1~5순위 카드·슬롯 드릴다운·제약완화 패널), 화면4(확정·공유: 예비순위 안내·링크/텍스트 공유). `screens/result/`, `screens/confirm/`
- **트랙 E** — 화면5(운영: 1클릭 다음순위 이동·변경 이력 타임라인) + `SUBMISSION.md`(3대 질문 답변)·`README.md`·`.github/workflows/deploy.yml`(GitHub Pages). `screens/operate/`

### git 정리 (worktree 격리 불가로 인한 히스토리 오염 수습)
- 단일 working tree 공유로 `track-d` 브랜치에 타 트랙 커밋이 섞임. 파일 경로는 트랙별 분리돼 손실 없음.
- 통합: master에 **track-c 병합 → track-e 병합 → track-d의 깨끗한 2커밋(result `71cdf87`, confirm `5e1ee93`) cherry-pick**. 충돌 0.
- 병합 커밋: `a954bd0`(C), `ceb5779`(E), cherry-pick `e9e6751`·`a16b366`(D)

### 품질 게이트 (통합 master에서 재검증)
- tsc strict **0 errors** / vite build **성공**(JS 214KB gz71KB) / vitest **9/9**

### 다음 단계
- 실제 dev 서버로 5화면 흐름·시나리오 5종 동작 검증(브라우저)
- 시각 디테일 폴리시(M3) → 배포·SUBMISSION 최종화(M4)

---

## 2026-06-30 · MEETSYNC 코어 v1→v2 재작업

### Slot 모델 전환
- 1시간 슬롯 → **30분 블럭** 구조로 전환: `day` + `blockIndex`(0~15, 하루 16블럭)
- 점심 블럭(blockIndex 6·7, 12:00~13:00) 자동 제외
- 회의 길이 가변: `durationMinutes` 30/60/90/120 = 연속 블럭 합성 채점
- 기간: `dateRange` 영업일 범위 도입
- 장소 목록: `Room.available: SlotKey[]` 도입(오프라인 회의실 가용 슬롯 명시)

### recommend.ts 재작업
- 연속 블럭 점유 합성 평가 로직 구현
- 오프라인 회의실 매칭 로직 추가
- tie-break / 다양성 / 제약완화 4종 재작업

### scenarios.ts 재작업
- 부록A 5종 시나리오를 30분 블럭 구조로 재구성 (날짜: 2026-06-29~07-03, 기본 60분 회의)

### store 추가 상태 및 액션
- `finalChoice` 상태 추가
- 신규 액션: `setDuration` / `setDateRange` / `addRoom` / `removeRoom` / `setFinalChoice`
- 신규 셀렉터 훅: `useFinalChoice`

### 신규 파서 모듈
- `src/lib/nlParser.ts` — 한국어 규칙기반 자연어 파서
- `src/lib/nlParserApi.ts` — claude-haiku-4-5 API 우선, 로컬 폴백 방식

### 품질 게이트
- 테스트 **17/17 통과** (recommend 11, nlParser 6)
- 담당 범위 TS 에러 **0** (잔여 18건은 `src/screens/**` — 다른 트랙 담당)

### 커밋 이력
- `181e837` types
- `edabe27` recommend
- `803cb74` scenarios + store
- `612caee` nlParser + tests

---

## 2026-06-30 · 화면(screens) v2 API 적응

- `src/screens/**` 9개 파일을 v2(30분 블럭) 도메인에 정합: `candidate.slot→startSlot`(표시 `formatRange`), `Slot.startHour→blockIndex`, `config.rangeDays→businessDayCount`.
- SlotGrid/TransparencyBoard 격자를 시(hour) 순회 → `VALID_BLOCKS`(16블럭) 순회로 전환, 라벨은 `blockStartLabel`/`formatBlock`.
- 게이트 전부 통과: tsc 0 · build OK · vitest 17/17. 커밋 `50979b4`.

---

## 2026-06-30 · 화면 v2 신규 기능 + 통합 정리

### 신규 기능 (화면 2트랙 병렬, opus)
- **화면1 생성** — 회의 길이 칩(30/60/90/120·`setDuration`), 직접 구현 월 그리드 **날짜범위 캘린더**(`DateRangeCalendar`), 오프라인 **장소 목록 관리**(`RoomList`). `screens/create/`
- **화면2 제약입력** — 탭 전환(`ModeTabs`): **버튼형**(셀 클릭 → `StatusPopover`로 가능/회피/불가 *선택*, 순환 아님) + **자연어 대화형**(`ConstraintChat`, `parseConstraints` async, 격자 즉시 반영). `screens/constraints/`
- **화면3 추천** — 투명성 보드 16블럭 정상화, 카드 시간 `formatRange`. **화면5 운영** — 1~5순위 전체 나열 + `setFinalChoice` 라디오 최종선택(`role=radiogroup`·키보드 탐색). `screens/result/`·`operate/`

### git 통합 정리 (worktree 격리 불가로 인한 다중 브랜치 수습)
- 병렬 세션들이 단일 워킹트리에서 브랜치를 옮겨다녀 갈래 다수 발생.
- `scr-q`(tip `29a7f86`)가 core v2 + 화면1~5 전부를 쌓은 **완전 통합본**임을 확인 → 전체 tsc 0·build·17/17 검증 후 master로 **fast-forward**, 잔여 브랜치 4개 삭제. 단일 브랜치로 정리.

### 교훈 / 운영 변경
- 이 환경은 worktree 격리 불가 → 다음 라운드부터 **에이전트는 파일만 수정, git 커밋·브랜치는 메인이 전담**.

---

## 2026-07-01 · GitHub 업로드 + 제약입력 개선

### GitHub private repo
- `gh repo create`로 private 저장소 생성·푸시: https://github.com/Mydefinary/toss-designer-challenge-2026 (master, .env/시크릿 미추적 확인)

### 제약입력 UX 개선 (opus, 파일만 수정 → 메인이 커밋)
- **점심 블럭 표시**: `VALID_BLOCKS` 0~17 전체(18블럭)로 확장(`BLOCKS_PER_DAY=18`), 점심 5·6·7(11:30–13:00)을 격자에 다시 포함. `LUNCH_BLOCKS=[5,6,7]` + `makeConstraintLookup`이 명시 제약 없는 점심칸을 **기본 불가(사유 '점심')로 합성** → 시드 무수정, override 가능. 후보 수 80→75.
- **인라인 선택**: 바텀시트(`StatusPopover`) 제거 → 상단 **브러시 팔레트**(`BrushPalette`: 가능/회피/불가 + 불가 시 사유 인라인). 셀 클릭/드래그로 현재 브러시 즉시 적용, 새 창 없음. 자연어 챗 모드 유지.
- 게이트: tsc 0 · build OK · vitest **20/20**(점심 테스트 3개 추가). 커밋 `6f51ea9` → origin push.
- 잔존 dead code: `constraints/ReasonPicker.tsx`(미사용) — 추후 정리 후보.

---

## 2026-07-03 · 참석자 편집·보드 개선·Claude 프록시·완화 강화

### GitHub
- private repo push 진행 중: https://github.com/Mydefinary/toss-designer-challenge-2026

### 참석자 편집 + 화면 잘림 수정 (커밋 `66743b3`)
- 생성 화면: 이름 직접 입력, 인원 추가/삭제(2~12명, 삭제 시 제약 정리·recompute), 6명 하드코딩 제거
- 제약 탭: 가로 스크롤 → 3열 그리드, 자연어 예시칩 5개
- 투명성 보드: 축 전환(열=참석자·행=시간블럭)으로 모바일 가로 잘림 해결
- vitest 26/26

### Claude 자연어 파서 실연동 (프록시 방식)
- **프론트**(커밋 `f017417`): `nlParserApi.ts`에서 브라우저 직접 Anthropic 호출 제거 → 백엔드 프록시(`VITE_PARSE_ENDPOINT`) 경유, 미설정/실패 시 로컬 파서 폴백. 키 브라우저 노출 0. `.env.example`·README·vite-env 갱신
- **백엔드**(별도 repo `C:\개발\정치불신`, FastAPI): `POST /api/meetsync/parse-constraints` 프록시 라우터. 서버 env `anthropic_api_key`로 httpx 호출, 키 없으면 503. 신규 파일 2개만 분리 커밋(`23c55ec`), config/main 반영은 `docs/MEETSYNC_PROXY.md`로 인수인계(사용자 observability 작업과 섞여). push 보류
- 배포 시: 프론트 `VITE_PARSE_ENDPOINT`, 백엔드 `.env` `ANTHROPIC_API_KEY`·`MEETSYNC_ORIGIN` 설정 필요

### 추천/완화 강화 + 문구 (커밋 `23ce8f8`)
- 오프라인인데 점유 블럭 전체를 커버하는 회의실이 없으면 **후보에서 제외(Hard)** — `ROOM_PENALTY` 제거
- 완화 제안에 **회의실 확보(`secure-room`)** + 온라인 전환 추가(undo 가능), 시나리오4 정합
- 확정 화면 문구 "일정이 확정됐어요" → **"최적 일정을 결정했어요"**
- vitest 29/29
- 운영 방식: 이번 라운드부터 **에이전트는 파일만 수정, git 커밋·push는 메인이 전담**(worktree 격리 불가 대응) — 안정적으로 동작 확인

---

## 2026-07-04 · 상황 공유·코멘트(백엔드 연동) + 경로 배포

### 화면3 상황 공유 + 코멘트 (실제 백엔드/DB)
- **프론트**(커밋 `71197f5`): 화면3 "이 상황 공유하기"(확정 전 1~5순위+모두의상황 스냅샷 저장→`#/shared/{id}` 링크 복사), `/shared/:id` 읽기전용 열람 화면(RankCard·TransparencyBoard 재사용), 순위별 👍/👎 + 코멘트 작성·목록. `src/lib/shareApi.ts`
- **백엔드**(정치불신, 커밋 `a79b368`): `MeetsyncShare`/`MeetsyncComment` 모델 + alembic 마이그레이션 `a1b9c7d3e5f2`(down_revision `7b2f23a7b2cc`) + 4개 엔드포인트(`POST/GET /shares`, `POST/GET /shares/{id}/comments`). snapshot은 JSON 저장. `docs/MEETSYNC_PROXY.md`에 스키마·적용법 인수인계
- **DB 적용 필요**: `docker compose exec backend alembic upgrade head && docker compose restart backend`

### 경로 배포(/meetsync) 설정 (커밋 `68c28fc`)
- vite `base`: 프로덕션 `/meetsync/` · 개발 `/`. dev `server.proxy` `/api`→localhost:8000
- API 클라이언트(shareApi·nlParserApi) **same-origin 상대경로 통일** → CORS 불필요. VITE_API_BASE 비우면 상대경로, 다른 오리진일 때만 설정
- README에 nginx `location /meetsync/` 예시·배포 절차

### 정치불신 저장소 상태
- meetsync 커밋 2개(`23c55ec` 파서 프록시, `a79b368` 공유·코멘트)는 **로컬 보존** — 저장소에 원격(origin) 미설정이라 push 불가. 원격 추가 시 push 예정
- `config.py`·`main.py`의 파서 프록시 반영은 사용자 observability 미커밋 작업과 섞여 커밋 안 함 → `docs/MEETSYNC_PROXY.md`로 인수인계
- `backend/nul`(에이전트 실수 생성 파일)은 사용자가 직접 정리 예정

---

## 2026-07-04 · MEETSYNC 독립 백엔드 분리 + 정치불신 통합 배포

### 독립 백엔드 분리 (커밋 `9cb810d`)
- 정치불신 저장소에 얹혀있던 프록시 방식을 정리하고, MEETSYNC 자체 저장소에 **독립 FastAPI 백엔드**(`server/`) 신설: 포트 8010, 별도 DB `meetsync`(정치불신 DB와 분리)
- `server/app/`: parse-constraints Anthropic 프록시 + shares/comments CRUD, 기동 시 `create_all`
- `server/Dockerfile`·`server/docker-compose.yml`·`server/requirements.txt`·`server/README.md`·`server/.env.example` 추가 (`server/.env`·`server/.venv`는 gitignore로 시크릿 미추적 확인)
- 프론트: `vite.config.ts` dev 프록시 → `localhost:8010`, `base=/meetsync/` 유지

### 정치불신 도커 스택 통합
- 정치불신 `docker-compose.yml`에 `meetsync-backend` 서비스 추가(별도 repo 소스 참조), nginx가 `/meetsync/`(정적)·`/api/meetsync/*`(→8010) 프록시
- 정치불신 frontend 이미지 빌드 시 MEETSYNC dist(`base=/meetsync/`)를 베이크해 같이 서빙
- 실 Anthropic API 키로 배포 후 자연어 제약 파서 동작 확인 완료

### 커밋
- `9cb810d` — server/ 독립 백엔드 + vite 프록시 + 배포_TODO.md (MEETSYNC repo, master 브랜치, push 안 함)

---

## 2026-07-04 · 회의 목록 생성 플로우 낙관적 UI 적용

### 수정 파일
- `src/screens/meetings/MeetingListScreen.tsx`
- `src/screens/meetings/MeetingListScreen.module.css`

### 낙관적 UI 도입
- `ListItem` 타입(`MeetingSummary & { pending?: boolean }`) 도입, `list` 상태를 `ListItem[]`로 변경
- `creating` boolean / 중복 클릭 가드 제거 → 동시 다중 낙관적 생성 허용
- 생성 클릭 시 `temp:${uuid()}` 임시 카드를 목록 맨 앞에 prepend, 성공 시 실제 id로 reconcile(`map`) 후 navigate, 실패 시 rollback(`filter`) + inline `createError` + 토스트
- 토스트 상태 / `timerRef` / `showToast`(2600ms) / 언마운트 정리 effect 추가 (SharedView 패턴)

### CSS
- `.chipDisabled` 제거
- `.meetingCardPending` / `.meetingPendingLabel` / `.toast` 추가 (토큰만 사용)

### 품질 게이트
- tsc 0 errors · build 성공 · test 31/31 통과

---

## 2026-06-27 · 회의 프리셋 저장/불러오기 기능 추가

### 신규 파일
- `src/lib/presetsApi.ts` — 프리셋 백엔드 API 클라이언트(listPresets/createPreset/getPreset/deletePreset). shareApi 스타일의 getBase/request 재사용, ownerToken은 meetingsApi와 공유. deletePreset은 204/빈 본문 허용.
- `src/lib/presetsApi.test.ts` — vitest 5개 테스트(목록/생성/단건/삭제/에러).

### 수정 파일
- `src/screens/ConstraintsScreen.tsx` — 하단 CTA 아래 "프리셋으로 저장" 폼 + 토스트 추가. getMeetingData() 스냅샷을 createPreset으로 저장.
- `src/screens/constraints/ConstraintsScreen.module.css` — 프리셋 저장 폼/토스트 스타일 추가.
- `src/screens/meetings/MeetingListScreen.tsx` — "프리셋에서 만들기" 섹션(목록 로드/프리셋으로 회의 생성/삭제) 추가.
- `src/screens/meetings/MeetingListScreen.module.css` — 프리셋 섹션 스타일 추가.

### 품질 게이트
- tsc 0 errors · build 성공 · vitest 36/36 통과
