# 작업내역

토스 프로덕트 디자이너 챌린지 2026 — "6명이 회의 일정을 잡는 경험 설계" 과제

---

## 2026-06-27 ~ 06-28 · 기획/PRD 수립

### 프로젝트 셋업
- `git init`으로 저장소 생성, `.gitignore` 추가(node/vite/env)
- 메모리 저장: 코딩 위임 정책(opus/sonnet + git worktree 병렬), 한글 응답, 프로젝트 개요

### 과제 분석
- 토스 디자이너 챌린지 2026 페이지 분석 → 주제·미션·평가축(문제정의·솔루션설계·시각완성도)·마감(2026-07-12 23:59) 정리
- 산출물 형태: **반응형 웹 인터랙티브 프로토타입**(GitHub Pages) + 3가지 질문 답변

### PRD.md 작성 및 고도화 (누적)
1. **초안** — 문제정의·솔루션·화면·기술스택·작업분해·마일스톤 기본 골격
2. **4대 변수 도메인 모델 반영** — ① 참석 중요도(필수/선택) ② 참석 가능 3단계(불가·회피·가능) ③ 회의 장소 유무 ④ 사후 이슈 대비
   - **1~5순위 예비 시간(backup ranking)** 컨셉을 핵심 차별점으로 도입 — 이슈 발생 시 다음 순위로 즉시 전환
   - 플랫폼 **웹** 확정
3. **"투명성이 양보를 만든다" 설계 철학 격상** — 정보 비대칭 제거가 자발적 양보를 유도
   - 투명성 보드(6명 전원 상황 히트맵), 슬롯 드릴다운(특정 시간이 왜 부적합한지) 추가
4. **핵심 통증 보강** — "결정 근거의 부재"를 가장 본질적 통증으로 추가(통증 6가지)
5. **데모 시나리오 5종(부록 A)** — 전형적 충돌/완벽한 시간 없음/필수자 충돌/장소 병목/이슈 대응. 각기 다른 엣지케이스 검증
6. **토스 공식 디자인 토큰(5.1)** — Toss Blue `#0064FF`, Toss Gray `#202632` + 확장 팔레트·타이포(Pretendard)·radius/shadow (출처: brand.toss.im)
7. **불가 사유 확장** — `회의`(선약) 태그 추가 + `기타`(직접 입력) 자유 텍스트 사유 기능

### 커밋 이력
- `52cf83a` 과제 분석 및 PRD 작성
- `cb5abd7` 4대 변수 반영, 1~5순위 백업, 웹 확정
- `b04ffa2` 투명성 철학·투명성 보드·슬롯 드릴다운
- `b7776ad` '결정 근거의 부재' 통증 추가
- `126061b` 데모 시나리오 5종 + 토스 디자인 토큰
- `ca4c2e7` 불가 사유 '회의' + 직접 입력(기타)

---

## 2026-06-28 · 구현 1차 (트랙 A+B)

### PRD 빈틈 보강(추가)
- 과제 원문·3대 질문 정확 문구 반영, "다음 주까지" 시급성 추가
- 점수모델 구체화(가중치·tie-break·순위 다양성), 제약완화 제안 **풀버전**(4종 완화·gain/cost 정렬·2단계 조합)
- 응답수집 스코프·반응형 투명성 보드·비범위(10장) 명시

### 트랙 A+B 구현 (opus 서브에이전트 위임)
- **worktree 격리 실패**(한글 경로 추정) → 단일 에이전트 비격리 백그라운드로 진행, 별도 브랜치 커밋 후 메인 병합
- 브랜치 `track-ab-scaffold-algo` (3 커밋) → master 병합 `c867640`
- 스캐폴딩: Vite+React+TS(strict), HashRouter, 디자인 토큰(tokens.css, Pretendard), 공용 UI 프리미티브(Button/Chip/Card/Badge/Avatar/AvatarStack), 화면 5종 stub
- 도메인: `src/types.ts`, `src/lib/recommend.ts`(슬롯생성·점수·tie-break·다양성·`suggestRelaxations` 풀버전), `src/data/scenarios.ts`(5종 시드)
- 품질 게이트 전부 PASS: tsc strict 0 errors / build OK / **테스트 9/9**

### 다음 단계
- 2차-①: 앱 셸 + 전역 스토어(Zustand) + 라우팅 흐름 + 시나리오 셀렉터 (opus 선행)
- 2차-②: 셸 병합 후 화면 트랙 C(생성/제약입력)·D(추천/확정)·E(운영+문서·배포) 병렬

---

## 2026-06-28 · 구현 2차 (트랙 track-shell-store)

### Zustand v5 전역 스토어 도입

- `npm i zustand` 설치(v5)
- `src/store/meetingStore.ts` 신설 — 단일 소스 상태(scenarioId/config/attendees/constraints)에서 `recommend()`·`suggestRelaxations()` 파생값(candidates/relaxations) 산출
- 확정 흐름: confirmedRanking·currentRankIndex 관리, 이슈 로그(issueLog), 완화 undo 스택(appliedRelaxations)
- 액션 9종: loadScenario / setConfig / setAttendeeRole / setConstraint / recompute / confirm / moveToNextRank / applyRelaxation / undoRelaxation
- 순수 헬퍼 `buildScenarioState` / `deriveCandidates` 로 초기화·액션 공유, `structuredClone` 깊은복사
- 셀렉터 훅 10종 + `useScenarioMeta` / `useMeetingActions`(`useShallow` 안정화)
- 배럴: `src/store/index.ts`

### 모바일 셸 구현

- `src/components/AppShell.tsx` + `AppShell.module.css` 신설
  - 최대폭 420px 고정, 자체 워드마크 **MEETSYNC**
  - 시나리오 셀렉터(`loadScenario` 연동)
  - 1~5 단계 인디케이터(`useLocation` 기반)
- `src/App.tsx` 내 Routes 전체를 AppShell로 래핑

### 화면 스텁 스토어 배선

- 5개 화면 스텁에 스토어 값 최소 배선 완료 (본문 UI는 트랙 C/D/E 담당)
- `global.css`에 `.screen-stub` / `.stub-meta` 스타일 추가

### 품질 게이트

- tsc 0 errors, vitest 9/9, vite build 성공

### 커밋 이력

- `d4adc09` zustand 스토어(meetingStore + 배럴)
- `9b9dbc3` 모바일 셸·라우팅·화면 배선·스타일
