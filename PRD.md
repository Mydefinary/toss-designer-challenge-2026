# PRD — 회의 일정 조율 경험 설계

> 토스 프로덕트 디자이너 챌린지 2026 과제 해결을 위한 제품 요구사항 정의서
> 작성일: 2026-06-27 · 제출 마감: 2026-07-12 23:59

---

## 0. 과제 개요

| 항목 | 내용 |
|------|------|
| **주제** | "6명이 회의 일정을 잡는 경험을 설계해주세요" |
| **미션** | 여러 동료의 일정 제약(점심 직후 회피, 특정 요일 외근, 필수/선택 참석 등)을 파악해, 모두에게 적합한 **1시간** 회의 시간을 찾아 일정을 잡는 솔루션 설계 |
| **제출물** | ① 3가지 질문 답변(발견한 문제·해결 방법·설계 근거) ② 사용 흐름 링크(Figma/웹/GitHub/영상) ③ 구글드라이브 링크 불가 |
| **평가 기준** | 문제 정의 · 솔루션 설계 · 시각적 완성도 |
| **형태** | 모바일·PC·태블릿·웹·앱 자유 / AI 도구 사용 가능 / 포트폴리오 불필요 |
| **금지** | 문제 정의 없이 결과 화면만 제안 / 기존 화면 모방 |

### 제출 산출물 결정
- **형태**: 반응형 **웹 인터랙티브 프로토타입**(실제 동작하는 GitHub Pages 링크) + 3가지 질문 답변 문서.
- **근거**: "사용 흐름을 볼 수 있는 링크"를 가장 강하게 만족하는 것은 실제 클릭 가능한 동작물. AI 도구 사용이 허용되므로 코드 구현으로 차별화.

---

## 1. 문제 정의 (평가축 1)

### 1.1 발견한 본질적 문제
기존 일정 조율 도구(When2meet, 단톡방 "언제 되세요?", 캘린더 수동 비교)는 **"빈 시간을 찾는 문제"로 접근**한다. 그러나 6명 회의의 실제 고통은 빈 시간 찾기가 아니라 **"제약을 가진 사람들 사이에서 누가 무엇을 양보할지 결정하는 협상"** 이다.

핵심 통증 4가지:
1. **제약의 비대칭성** — 점심 직후 회피(선호), 화요일 외근(불가), 필수/선택 참석(가중치)은 서로 무게가 다른데 기존 도구는 모두 똑같은 "가능/불가" 이진값으로 뭉갠다.
2. **조율자의 인지 부하** — 주최자가 6명 ×시간대 격자를 머릿속에서 교차 비교. 사람이 늘수록 조합 폭발.
3. **완벽한 시간의 부재** — 6명 모두 100% 만족하는 슬롯은 거의 없음. 도구는 "겹치는 시간 없음"에서 멈추고, 사람이 다시 수동 협상으로 떨어진다.
4. **응답 수집의 마찰** — 참석자가 자기 일정을 일일이 입력해야 하고, 미응답자 때문에 결정이 무한 지연.

### 1.2 왜 이것이 본질인가
과제 단서(점심 직후/외근/필수·선택)는 의도적으로 **"단순 빈칸 찾기로는 풀리지 않는 충돌"** 을 심어둔 것. 즉 출제 의도는 *"제약 충돌을 어떻게 드러내고 의사결정하게 할 것인가"* 를 묻는 것이다.

---

## 2. 솔루션 설계 (평가축 2)

### 2.1 핵심 가설
> **"가능/불가 이진값 대신 '제약의 무게'를 시각화하고, 최적이 아닌 '최선의 타협안'을 시스템이 먼저 제안하면, 조율자는 협상 부담 없이 빠르게 결정할 수 있다."**

### 2.2 솔루션 컨셉: "양보가 보이는 일정 조율"
빈 시간을 찾아주는 게 아니라, **각 후보 시간이 누구에게 어떤 부담을 주는지(trade-off)** 를 보여주고 1순위 추천을 제시한다.

### 2.3 제약 모델 (3단계 가중치)
| 제약 유형 | 예시 | 가중치 처리 |
|-----------|------|-------------|
| **절대 불가 (Hard)** | 화요일 외근, 연차 | 해당 슬롯 후보 제외 |
| **선호 회피 (Soft)** | 점심 직후 졸림, 오전 집중업무 | 패널티 점수, 후보 유지 |
| **참석 중요도** | 필수 / 선택 참석자 | 필수의 제약은 가중↑, 선택은 가중↓ |

### 2.4 추천 알고리즘 (개념)
1. 모든 1시간 슬롯 생성 → Hard 제약 슬롯 제거
2. 각 슬롯 점수 = Σ(필수자 만족도 × W_required) + Σ(선택자 만족도 × W_optional) − Σ(Soft 패널티)
3. 상위 3개 슬롯을 "추천 / 차선 / 대안"으로 제시, 각각 **누가 어떤 양보를 하는지** 명시

### 2.5 핵심 사용자 흐름
```
[주최자] 회의 만들기 → 참석자 6명 추가(필수/선택 지정)
   → 제약 입력(직접 or 링크로 수집)
   → 시스템이 후보 시간 3개 + trade-off 카드 제시
   → 1개 선택(누가 양보하는지 확인 후) → 확정·공유
```

### 2.6 주요 화면 (MVP 범위)
1. **회의 생성** — 제목, 길이(1h 고정), 기간 범위, 참석자 6명(필수/선택 토글)
2. **제약 입력** — 참석자별 불가/선호 회피 빠른 입력(요일·시간대 칩)
3. **추천 결과 (핵심 화면)** — 후보 슬롯 카드 3개. 각 카드에 만족 인원 수, "양보하는 사람"과 사유를 시각적으로 표시. 6명 ×시간 히트맵 보조.
4. **확정·공유** — 선택 슬롯 확정, 요약 카드 공유.

---

## 3. 설계 근거 (평가축 3)

| 결정 | 근거 |
|------|------|
| 빈칸 찾기 → 양보 시각화로 프레임 전환 | 본질 문제가 "탐색"이 아닌 "협상"이라는 정의에서 직접 도출 |
| 추천 슬롯 3개 + trade-off 명시 | 완벽한 시간 부재 문제 해결. 사람이 "왜 이 시간?"을 납득하게 함 |
| 필수/선택 가중치 분리 | 과제 단서를 정직하게 모델링. 의사결정 우선순위 제공 |
| 모바일 우선 + 토스 디자인 언어 | 평가축 "시각적 완성도" + 회의 조율은 이동 중 모바일에서 자주 발생 |

---

## 4. 시각적 완성도 (평가축 3)

- **디자인 언어**: 토스 톤(절제된 컬러, 큰 타이포, 명확한 위계, 넉넉한 여백). Primary는 토스 블루 계열.
- **핵심 UI 패턴**: 후보 슬롯 = 카드, 제약 = 칩(chip), 만족도 = 아바타 스택 + 색상, 비교 = 히트맵.
- **마이크로인터랙션**: 슬롯 선택 시 양보자 하이라이트 트랜지션, 확정 시 컨페티/체크 모션.
- **반응형**: 모바일(375px) 우선, 태블릿/데스크톱 대응.

---

## 5. 기술 스택 / 구현 계획

| 영역 | 선택 | 근거 |
|------|------|------|
| 프레임워크 | **React + Vite + TypeScript** | 빠른 프로토타이핑, GitHub Pages 배포 용이 |
| 스타일 | Tailwind CSS or CSS Modules | 토스풍 디자인 토큰 관리 |
| 상태 | 로컬 상태(Zustand 또는 React state) | 백엔드 없는 클라이언트 데모 |
| 데이터 | 목업 시드 데이터(6명 페르소나 + 제약) | 즉시 체험 가능한 데모 시나리오 |
| 배포 | GitHub Pages | 제출용 공개 링크 |
| 추천 로직 | 클라이언트 스코어링 함수(`src/lib/recommend.ts`) | 알고리즘 캡슐화·테스트 가능 |

### 5.1 데모 시드 시나리오 (6명 페르소나)
출제 단서를 그대로 반영한 현실적 충돌 케이스 1개를 기본 탑재 — 화요일 외근자, 점심직후 회피자, 필수 3명/선택 3명 등.

---

## 6. 작업 분해 (병렬 워크트리 기준)

> 코딩은 opus/sonnet 서브에이전트가 git worktree 격리로 병렬 진행. 영역별 파일 분리.

| 트랙 | 담당(모델) | 범위 | 주요 파일 |
|------|-----------|------|----------|
| A. 프로젝트 스캐폴딩 | opus | Vite+React+TS 셋업, 라우팅, 디자인 토큰 | `vite.config`, `src/main`, `src/styles` |
| B. 추천 알고리즘 + 데이터 모델 | opus | 제약 모델, 스코어링, 시드 페르소나 | `src/lib/`, `src/data/` |
| C. 화면 1·2 (생성/제약 입력) | sonnet | 회의 생성, 제약 칩 입력 UI | `src/screens/Create`, `Constraints` |
| D. 화면 3·4 (추천/확정) | opus | 핵심 추천 카드·히트맵·공유 | `src/screens/Result`, `Confirm` |
| E. 답변 문서 + 배포 | sonnet | 3가지 질문 답변, GitHub Pages 배포 | `SUBMISSION.md`, CI |

*동일 파일은 한 트랙만 수정. 공유 타입(`src/types.ts`)은 트랙 B가 먼저 확정 후 다른 트랙 시작.*

---

## 7. 마일스톤

| 단계 | 산출물 | 기한 |
|------|--------|------|
| M1 | PRD 확정 + 스캐폴딩 + 타입/알고리즘 | ~6/30 |
| M2 | 4개 화면 동작 프로토타입 | ~7/5 |
| M3 | 시각 완성도 폴리시 + 데모 시나리오 | ~7/9 |
| M4 | 답변 문서 + 배포 + 최종 점검 | ~7/12 |

---

## 8. 성공 기준 (검증 지표)

- [ ] 6명 제약 입력 → 1시간 추천 슬롯 3개가 trade-off와 함께 표시된다
- [ ] 출제 단서(점심직후/외근/필수·선택)가 모두 데모에서 작동·시각화된다
- [ ] 모바일에서 생성→확정까지 끊김 없이 흐른다
- [ ] 3가지 질문 답변이 문제→해결→근거 논리로 연결된다
- [ ] 공개 링크가 외부에서 즉시 열린다(구글드라이브 아님)
