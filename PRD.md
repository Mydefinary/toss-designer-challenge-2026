# PRD — 회의 일정 조율 경험 설계

> 토스 프로덕트 디자이너 챌린지 2026 과제 해결을 위한 제품 요구사항 정의서
> 작성일: 2026-06-27 · 최종 수정: 2026-06-28 · 제출 마감: 2026-07-12 23:59
> **플랫폼: 웹 (반응형, 모바일 우선)**

---

## 0. 과제 개요

### 과제 원문 (출처: toss.im/career/designer-challenge-2026)
> "같은 회사 동료 **6명**이 **다음 주까지** 모여야 해요. 회의 시간은 딱 **1시간**이에요. 누군가는 **점심 직후를 피하고 싶어**하고, 어떤 사람은 **특정 요일에 외근**이 많아요. **꼭 참석해야 하는 사람**도 있고, **일부 인원은 선택 참석**이어도 괜찮아요."

| 항목 | 내용 |
|------|------|
| **주제** | "6명이 회의 일정을 잡는 경험을 설계해주세요" |
| **미션** | 위 단서의 일정 제약을 파악해, **다음 주(영업일 5일) 안에** 모두에게 적합한 **1시간** 회의 시간을 찾아 일정을 잡는 솔루션 설계 |
| **제출물** | ① **3가지 질문 답변**(아래) ② 사용 흐름 링크(Figma/웹/GitHub/영상) ③ 구글드라이브 링크 불가 |
| **평가 기준** | 문제 정의 · 솔루션 설계 · 시각적 완성도 (아래 상세) |
| **형태** | **웹** (반응형) / AI 도구 사용 가능 / 포트폴리오 불필요 |
| **금지** | 문제 정의 없이 결과 화면만 제안 / 기존 화면 모방·시각 스타일만 개선 |

**제출 3가지 질문 (정확 문구 — SUBMISSION.md는 이 순서·문구로 답한다)**
1. "사용자의 어떤 문제를 발견했나요? 그것이 왜 문제라고 생각했나요?"
2. "그 문제를 어떻게 해결했나요?"
3. "왜 이 방식으로 설계했나요?"

**평가 기준 상세 (원문)**
- **문제 정의**: 주어진 단서와 맥락을 바탕으로 사용자가 겪는 **본질적인 문제를 발견·정의**했는지.
- **솔루션 설계**: 정의한 문제와 솔루션이 설계안에 잘 반영됐는지. **명확한 가설·의도**로 사용자가 쉽게 이해하고 목적을 달성하는 UX인지.
- **시각적 완성도**: 사용성 + 심미·조형적 완성도. **익숙한 UI 패턴도 맥락에 맞게 다듬어** 쓰고 시각 디테일로 완성도를 높이는지.

> 참고: "토스 앱과 연관 지어 생각하지 않아도 된다"(원문). → 토스 브랜드 톤은 *차용*하되 토스 기존 화면을 모방하지 않고 **독립 제품**으로 설계.

### 제출 산출물 결정
- **형태**: 반응형 **웹 인터랙티브 프로토타입**(실제 동작하는 GitHub Pages 링크) + 3가지 질문 답변 문서.
- **근거**: "사용 흐름을 볼 수 있는 링크"를 가장 강하게 만족하는 것은 실제 클릭 가능한 동작물. AI 도구 사용이 허용되므로 코드 구현으로 차별화.

---

## 1. 회의에 영향을 끼치는 변수 (도메인 모델)

회의 일정 조율을 좌우하는 변수를 4가지로 구조화한다. 이 변수 정의가 솔루션 전체의 토대다.

### V1. 회의 대상자 — 참석 중요도
6명은 모두 동등하지 않다. **꼭 와야 하는 사람**과 **있으면 좋지만 없어도 되는 사람**이 섞여 있다.
- **필수 참석자(Required)** — 이 사람이 빠지면 회의 자체가 무의미. 제약을 강하게 반영.
- **선택 참석자(Optional)** — 참석하면 좋지만 불참해도 회의 성립. 제약을 약하게 반영.
- → **기능 요구**: 참석자별로 필수/선택을 토글로 구분. 추천 시 가중치 차등 적용.

### V2. 회의 대상자의 스케줄 — 참석 가능 상태 (3단계)
참석 가능 여부는 이진(가능/불가)이 아니라 **3단계 상태**로 모델링한다.

| 상태 | 의미 | 예시 | 추천 처리 |
|------|------|------|-----------|
| **불가 (Unavailable)** | 아예 참석 불가능 | 외근, 미출근, 퇴근 후, 휴가, 선약된 회의 | 해당 슬롯 **후보 제외** |
| **회피 선호 (Avoid)** | 가능하지만 피하고 싶음 | 점심 직후(졸림), 오전 집중업무 | **패널티 점수**, 후보 유지 |
| **가능 (Available)** | 문제없이 회의 가능 | — | 만족 점수 |

- → **기능 요구**: 참석자별로 시간대(요일×시간)마다 3단계 상태를 칩으로 입력. "불가"는 사유 태그(외근·휴가·퇴근후·미출근·**회의**) 중 선택하거나, **직접 입력(기타)** 으로 자유 텍스트 사유를 적을 수 있다. 우리가 정의한 태그 외의 사유(병원, 출장, 개인일정 등)도 누락 없이 기록 가능. 입력된 사유는 투명성 보드·슬롯 드릴다운에서 "왜 불가한지"로 그대로 노출된다.

### V3. 회의 장소 — 장소 유무 + 장소 목록
시간이 맞아도 **장소(회의실)** 가 없으면 회의는 성립하지 않는다. 기존 도구가 자주 빠뜨리는 변수.
- **장소 필요 여부**: 온라인 회의면 장소 무관, 오프라인이면 회의실 필요.
- **장소 목록 관리**: 오프라인 선택 시 **가능한 장소(회의실) 목록을 직접 추가/삭제**하고, 각 장소의 시간대 가용을 후보 제약으로 반영.
- → **기능 요구**: 회의 생성 시 "온라인/오프라인" 선택. 오프라인이면 장소 목록 입력 UI. 후보 슬롯에 **어떤 회의실이 가용한지 뱃지** 표시(가용 회의실 없으면 경고).

### V5. 회의 길이 · 후보 기간 (가변 설정)
과제 기본은 1시간이지만, 실제 제품은 회의 성격에 따라 길이가 다르다.
- **회의 길이**: 30/60/90/120분 선택(기본 60). 길이는 **30분 블럭의 연속 점유**로 계산.
- **후보 기간**: "다음 주" 고정이 아니라 **캘린더로 시작~종료 날짜 범위**를 선택. 선택 범위를 한눈에 보도록 캘린더 UI 제공.
- → **기능 요구**: 생성 화면에 길이 선택 + 기간 캘린더. 슬롯 생성·점수·추천이 이 설정을 따른다.

### V4. 사후 이슈 — 갑작스러운 불참 (★ 핵심 차별점)
일정을 확정한 뒤에도 참석자에게 이슈(긴급 외근, 컨디션, 일정 충돌)가 생겨 정한 시간에 못 오는 경우가 발생한다. 이때 처음부터 다시 조율하는 것은 큰 비용.
- **해결**: 확정 시점에 단일 슬롯이 아니라 **1~5순위 예비 시간(backup ranking)** 을 함께 확정해 둔다.
- **이슈 발생 시**: 현재 순위를 폐기하고 **다음 순위로 한 번에 이동** → 재조율 없이 즉시 의사결정.
- → **기능 요구**: 추천 결과를 순위 리스트(1~5순위)로 확정. 각 순위는 trade-off가 다름. "이슈 발생 → 다음 순위로" 액션 한 번으로 회의 시간 갱신·재공유.

---

## 2. 문제 정의 (평가축 1)

### 2.1 발견한 본질적 문제
과제에는 **"다음 주까지 모여야 한다"** 는 시간 압박이 깔려 있다. 즉 느긋한 최적화가 아니라 **영업일 5일 안에 빠르게 결정하고, 한 번 정하면 흔들리지 않아야 하는** 상황이다. 이 시급성이 "완벽한 시간 탐색"보다 "빠르고 견고한 의사결정"을 본질로 만든다.

기존 일정 조율 도구(When2meet, 단톡방 "언제 되세요?", 캘린더 수동 비교)는 **"빈 시간을 한 번 찾는 문제"** 로 접근한다. 그러나 6명 회의의 실제 고통은 두 층위다.

1. **결정의 어려움** — 제약이 가진 사람들 사이에서 누가 무엇을 양보할지 정하는 협상. 무엇보다 **판단할 근거가 보이지 않아** 결정 자체가 막힌다. (V1·V2·V3)
2. **결정의 취약함** — 어렵게 정해도 이슈 하나로 무너지고, 처음부터 다시 조율해야 함. (V4)

핵심 통증 6가지:
1. **결정 근거의 부재 (★ 가장 본질)** — 어떤 시간을 골라야 할지 **판단할 근거가 보이지 않는다**. 누가 더 곤란한지, 이 시간을 고르면 누가 무엇을 양보하는지, 왜 이 시간이 별로인지가 드러나지 않으니 결정 자체를 못 하고 미루거나 눈치 싸움으로 흐른다. (→ 투명성으로 해결)
2. **제약의 비대칭성** — 불가/회피/가능, 필수/선택은 무게가 다른데 기존 도구는 "가능/불가" 이진값으로 뭉갠다. (V1·V2)
3. **조율자의 인지 부하** — 주최자가 6명×시간대 격자를 머릿속에서 교차 비교. 사람이 늘수록 조합 폭발.
4. **완벽한 시간의 부재** — 6명 모두 100% 만족 슬롯은 거의 없음. 도구는 "겹치는 시간 없음"에서 멈추고 사람이 수동 협상으로 떨어진다.
5. **장소 누락** — 시간만 맞추고 회의실이 없어 다시 조율. (V3)
6. **결정의 휘발성** — 확정 후 한 명만 빠져도 전체 재조율. (V4)

### 2.2 왜 이것이 본질인가
과제 단서(점심직후/외근/필수·선택)는 의도적으로 **"단순 빈칸 찾기로는 풀리지 않는 충돌"** 을 심어둔 것. 출제 의도는 *"제약 충돌을 어떻게 드러내고, 흔들리지 않게 의사결정하게 할 것인가"* 를 묻는 것이다.

---

## 3. 솔루션 설계 (평가축 2)

### 3.1 핵심 가설
> **"모두의 상황을 투명하게 드러내면, 강제하지 않아도 자발적 양보가 만들어진다."**
> 이진 가능/불가 대신 '제약의 무게'를 시각화하고, 각 시간이 **누구에게 왜 부적합한지**까지 보여주며, '최선의 타협안'을 1~5순위로 미리 제안하면 — 조율자는 협상 부담 없이 빠르게 결정하고 이슈에도 흔들리지 않는다.

### 3.2 설계 철학: 투명성이 양보를 만든다 (★ 가장 중요)
양보를 *요구*하는 것이 아니라, **정보 비대칭을 없애 양보가 자연스럽게 일어나게** 한다.
- 사람들이 양보하지 않는 이유는 이기심이 아니라 **"내가 양보해야 하는지 모르기 때문"** 이다. 누가 더 곤란한지, 내 회피가 전체에 어떤 영향을 주는지 보이지 않는다.
- 따라서 솔루션의 1순위 목표는 **"모두의 상황을 한눈에 명확히 보여주는 것"**. 6명의 제약이 투명하게 펼쳐지면, "아, 이 시간은 나만 불편하고 나머지 5명은 다 되네 — 내가 양보하지"가 스스로 일어난다.
- 시스템은 **"누군가는 양보해야 한다"는 사실과, 그 양보의 주체·비용**을 숨기지 않고 정직하게 드러낸다.

### 3.3 솔루션 컨셉: "흔들리지 않는 일정" — 투명성·양보·순위 백업
세 축으로 차별화한다.
1. **(투명) 상황 가시화** — 6명 전원의 가능/회피/불가를 한 화면에 명확히. 정보 비대칭 제거.
2. **(결정) 양보·비용 가시화** — 각 후보 시간이 누구에게 어떤 부담을 주는지, 그리고 **그 시간이 왜 부적합한지(negative reason)** 까지 드릴다운으로 제시.
3. **(대비) 순위 백업** — 한 시간이 아니라 1~5순위를 확정. 이슈 발생 시 다음 순위로 즉시 전환.

### 3.4 제약·점수 모델 (구체화)
```
슬롯 점수 = Σ(필수자 만족도 × W_required)      // W_required = 3
          + Σ(선택자 만족도 × W_optional)      // W_optional = 1
          − 장소부재 패널티                     // 오프라인·회의실 없음 = −5

만족도 값:   가능 = +1,  회피 = −0.5,  불가 = (아래 제외 규칙)
Hard 제약:  필수자 1명이라도 '불가' → 후보 제외
            선택자가 '불가' → 그 슬롯에서 "불참 처리"(점수 0 기여, 후보 유지)
```
- **가중치 근거**: 필수자의 '회피'(−0.5×3=−1.5)가 선택자의 '가능'(+1×1)보다 무겁게 작동 → 필수자 편의 우선.
- **Tie-break(동점 정렬)**: ① 불가/회피 인원 적은 슬롯 → ② 필수자 전원 '가능'인 슬롯 → ③ 이른 날짜·시간(다음 주까지 시급성). 점수+타이브레이크로 안정 정렬.
- **순위 다양성 규칙(★)**: 상위 5개가 인접 슬롯에 몰리면 "백업"의 의미가 없다. **같은 날 최대 2개**까지만 순위에 포함, 가능하면 **양보 주체가 다른** 슬롯을 우선해 1~5순위가 서로 다른 trade-off를 갖도록 분산.

### 3.5 추천 알고리즘 (개념)
1. 기간(다음 주 영업일 5일) 내 1시간 슬롯 전부 생성
2. 필수자 '불가' 슬롯 제거 (+ 오프라인인데 가용 회의실 0개면 제외 또는 경고 뱃지)
3. 각 슬롯 점수 계산 → 내림차순 정렬 → Tie-break → 다양성 규칙으로 1~5순위 선별
4. 각 순위마다 **양보자**·장소 상태·부적합 사유를 부착
5. 사용자가 순위 리스트를 검토·조정 후 확정

**용어 — "양보자(yielding)"**: 해당 슬롯에서 (a) '회피' 상태인데도 포함된 사람, (b) 선택자인데 '불가'라 불참하게 되는 사람. 카드·드릴다운에 "○○님이 점심직후를 양보 / △△님은 불참"으로 구분 표기.

### 3.5.1 해(解)가 부족할 때 — 제약 완화 제안 (★ 투명성의 완성 / **풀버전 확정**)
필수자 충돌로 **후보가 0~2개로 희소**하거나 아예 없을 수 있다(부록 A 시나리오 3). 이때 "겹치는 시간 없음"에서 멈추지 않고, **"무엇을 풀면 회의가 가능해지는지"** 를 역으로 제안한다. 문제를 보여주는 데서 끝내지 않고 **결정 가능한 선택지**로 전환 — 투명성 철학의 논리적 완성.

**완화 후보(가상 완화 단위)**
1. **선택자 제외** — 선택 참석자 1명을 이번 회의에서 빼기.
2. **회피 무시** — 특정 참석자의 '회피' 1건을 양보로 전환(불가가 아니므로 원래 가능).
3. **Hard 제약 부분 조정** — 특정 참석자의 '불가' 1건을 조정(예: 외근 반나절 단축, 휴가 일부 시간 참석).
4. **장소 전환** — 오프라인→온라인으로 바꿔 회의실 제약 제거.

**풀버전 알고리즘**
```
base = 현재 후보 슬롯 집합 (제약 완화 전)
relaxations = 위 1~4 유형으로 생성한 개별 완화 액션 전체
for each action r in relaxations:
    candidates_r = 추천 알고리즘 재실행 (r 적용한 가상 제약으로)
    gain(r)      = |candidates_r| − |base|          // 후보 증가량
    bestRank(r)  = candidates_r 최상위 슬롯의 점수    // 완화 후 1순위 품질
    cost(r)      = 완화의 부담(불가>회피>선택자제외>온라인전환 순 가중)
정렬: gain 내림차순 → cost 오름차순 → bestRank 내림차순
상위 3개를 "이렇게 하면 풀려요" 카드로 제시
```
- **2단계 조합(옵션)**: 단일 완화로도 후보가 0이면, 완화 액션 2개 조합까지 탐색(조합 폭발 방지 위해 상위 영향 액션 N=6개 내에서 쌍으로만).
- **표현**: 각 제안 카드에 *"무엇을 / 누가 / 결과(후보 +N개, 새 1순위 = ○요일 △시)"* 를 명시. 사용자가 카드를 누르면 그 완화를 적용한 추천 결과로 즉시 전환(되돌리기 가능).
- **연결**: 부록 A 시나리오 3가 이 풀버전을 집중 검증.

### 3.8 자연어 대화형 제약 입력 (파서 기본 + API 선택)
"둘 다" 방식으로 구현한다.
- **기본: 로컬 규칙기반 파서**(`src/lib/nlParser.ts`) — 한국어 패턴 매칭. 추출 요소: 참석자(이름/별칭) · 요일(월~금) · 시간대(오전/오후/점심직후/특정시각/범위) · 상태 키워드(외근·휴가·퇴근·불가→unavailable / 피하고싶다·가능하면 피해→avoid / 가능·괜찮→available) · 사유. 한 문장에 여러 사람·여러 제약 허용. 결과를 `ConstraintCell[]`로 변환해 스토어에 반영하고, 사람이 읽는 확인 메시지를 돌려준다. 파싱 실패/모호 시 되묻기.
- **고도화: Claude API**(`src/lib/nlParserApi.ts`, 동일 인터페이스) — `VITE_ANTHROPIC_API_KEY` 환경변수가 있으면 사용. 모델 `claude-haiku-4-5`로 문장→구조화(JSON) 추출. 키 없으면 자동으로 규칙기반 폴백. **정적 배포 기본값은 파서**(키 노출 방지). 인터페이스: `parseConstraints(text, attendees, config): {cells: ConstraintCell[], message: string, unresolved?: string[]}`.

### 3.6 핵심 사용자 흐름
```
[주최자] 회의 만들기 (제목·1h·기간·온라인/오프라인)
   → 참석자 6명 추가 (필수/선택 토글)
   → 제약 입력 (참석자별 불가/회피/가능 + 사유)
   → 시스템이 1~5순위 후보 + trade-off·장소상태 제시
   → 순위 확정 → 공유
   ─────────── (이후 이슈 발생) ───────────
   → "○○님 불참" → [다음 순위로 이동] 한 번 클릭 → 재확정·재공유
```

**제약 입력 주체 (스코프 결정)**: 실제 제품 비전은 *주최자가 링크를 공유 → 참석자 각자 자기 제약 입력*(응답 수집)이다. 그러나 **이번 데모는 평가자가 즉시 전체 흐름을 체험**하도록 *주최자 1인 시점에서 6명 제약을 시드 데이터로 미리 채운 상태*로 시작한다. 참석자 개별 입력 화면은 동일한 '제약 입력' UI를 1인용으로 재사용하는 형태로 1개만 보여준다(전체 응답 수집 동기화는 데모 비범위 → 10장).

### 3.7 주요 화면 (MVP 범위, 웹)
1. **회의 생성** — 제목, **회의 길이(30/60/90/120분)**, **후보 기간(캘린더로 날짜 범위 선택)**, **온라인/오프라인 + 오프라인 시 장소 목록 추가/삭제**, 참석자 6명(필수/선택 토글)
2. **제약 입력 — 2가지 방식 (탭 전환)**
   - **(버튼형)** 참석자별 30분 블럭 격자. 셀을 **클릭하면 가능/회피/불가를 선택하는 팝오버**가 뜬다(클릭마다 순환 아님). 불가 선택 시 사유 태그(외근·휴가·퇴근후·미출근·회의)/직접입력(기타).
   - **(자연어 대화형)** 채팅 입력창에 "민준은 화요일 오후 외근, 서연은 점심 직후 피하고 싶어함"처럼 문장으로 입력 → 파싱해 해당 셀에 반영하고 "민준님 화 13:00–18:00 불가로 반영했어요" 식으로 확인. 기본은 로컬 규칙기반 파서, API 키 설정 시 Claude API로 고도화(3.8).
3. **추천 결과 (핵심 화면)** — 두 영역이 함께 보인다.
   - **(a) 투명성 보드** — 6명 전원 × 시간대 히트맵. 누가 언제 가능/회피/불가인지 한눈에. "모두의 상황"을 명확히 펼쳐 양보의 근거를 제공.
   - **(b) 1~5순위 후보 카드** — 각 카드: 만족 인원(아바타 스택), **양보하는 사람·사유**, 장소 가용 뱃지.
   - **(c) 슬롯 드릴다운(★)** — 특정 시간을 선택하면 **"이 시간이 왜 회의에 적합하지 않은지"** 를 펼쳐 보여준다: 누가 양보해야 하는지, 어떤 제약(점심직후/외근/회의실 없음)이 걸렸는지, 만족 점수가 깎인 이유. 긍정·부정 근거를 함께 노출해 납득 가능한 결정을 돕는다.
   - **(d) 제약 완화 제안** — 후보가 희소/전무할 때 "무엇을 풀면 가능한지"를 카드로 제안(3.5.1).
4. **확정·공유** — 순위 리스트 확정, 요약 카드 공유. *구현: 백엔드 없이 상태를 URL 쿼리로 인코딩한 공유 링크 + 요약 이미지(PNG) 다운로드 양쪽 제공.*
5. **운영(이슈 대응)** — 확정된 **1~5순위를 전부 나열**해 한눈에 비교(각 순위의 시간·양보자·장소). 그 중 **최종 선택을 직접 지정**(라디오/선택)하는 방식. 이슈가 생기면 다른 순위를 최종으로 다시 지정. 변경 이력·재공유.

---

## 4. 설계 근거 (평가축 3)

| 결정 | 근거 |
|------|------|
| **투명성 보드(6명 전원 상황 노출)** | "정보 비대칭이 양보를 막는다" — 모두의 상황이 명확하면 자발적 양보가 일어난다 |
| **슬롯 드릴다운(왜 부적합한지 노출)** | 추천을 강요하지 않고 부정적 근거까지 투명하게 보여줘 납득 가능한 결정 유도 |
| 빈칸 찾기 → 양보 시각화로 프레임 전환 | 본질 문제가 "탐색"이 아닌 "협상"이라는 정의에서 직접 도출 |
| **단일 슬롯 → 1~5순위 백업** | V4(사후 이슈)에 대응. 재조율 비용 제거가 가장 큰 차별점 |
| 가능 상태 3단계(불가/회피/가능) | V2를 정직하게 모델링. 이진값이 못 담는 "피하고 싶음"을 표현 |
| 필수/선택 가중치 분리 | V1 단서를 정직하게 모델링. 의사결정 우선순위 제공 |
| 장소 변수 명시 | V3. 시간만 맞추는 기존 도구의 사각지대 보완 |
| 모바일 우선 반응형 웹 | 평가축 "시각적 완성도" + 회의 조율은 이동 중 자주 발생 |

---

## 5. 시각적 완성도 (평가축 3)

- **디자인 언어**: 토스 톤(절제된 컬러, 큰 타이포, 명확한 위계, 넉넉한 여백). Primary는 토스 블루.
- **핵심 UI 패턴**: 후보 = 순위 카드, 제약 = 칩(chip), 만족도 = 아바타 스택 + 색상, 비교 = 히트맵, 장소 = 뱃지.
- **마이크로인터랙션**: 슬롯 선택 시 양보자 하이라이트, "다음 순위로 이동" 시 카드 승격 트랜지션, 확정 시 체크 모션.
- **반응형**: 모바일(375px) 우선, 태블릿/데스크톱 대응.
- **투명성 보드 반응형 전략(★ 난제)**: 6명 × 최대 40슬롯(5일×8) 히트맵은 375px에 한 번에 못 담는다.
  - **모바일**: 상단 **요일 탭(월~금)** 으로 하루씩(6명 × 8슬롯)만 표시 + 가로 스크롤 보조. 추천 순위 카드가 먼저, 보드는 접힘/펼침.
  - **태블릿/데스크톱**: 6명 × 5일 전체 히트맵을 한눈에. 열=시간, 행=사람.
  - 셀은 색(가능 블루 / 회피 앰버 / 불가 레드)+아이콘으로 색약 접근성 확보, 탭 시 사유 툴팁.

### 5.1 디자인 토큰 (출처: https://brand.toss.im/)
**공식 브랜드 컬러**
| 토큰 | 값 | 비고 |
|------|----|------|
| `--toss-blue` (Primary) | **#0064FF** | 공식. PANTONE 2175C / RGB(0,100,255) |
| `--toss-gray` (Text/Ink) | **#202632** | 공식. 본문·헤드라인 텍스트 기본색 |

**확장 팔레트 (토스풍 권장값 — 비공식, 가독성·위계용)**
| 역할 | 값 |
|------|----|
| Primary Hover/Pressed | #0050C8 / #0046B0 |
| Primary Tint (배경 강조) | #E8F1FF |
| 배경 Base | #FFFFFF / 보조 #F2F4F6 |
| Gray scale | #F9FAFB · #F2F4F6 · #E5E8EB · #C9CDD2 · #8B95A1 · #4E5968 · #202632 |
| 상태색 — 가능 ● | #0064FF (블루) |
| 상태색 — 회피 ▲ | #FF9500 (앰버, 경고) |
| 상태색 — 불가 ✕ | #F04452 (레드) |
| 성공/확정 | #00C880 |

**타이포그래피**: 한글은 **Pretendard**(오픈소스, 토스 실무 톤과 근접) 사용. 위계 — Display 28–34 / Title 20–24 / Body 15–16 / Caption 12–13, Bold·SemiBold 위주의 강한 위계.

**형태 토큰 (권장)**: Radius — 카드 16px / 버튼 12px / 칩 8px. Shadow — `0 2px 8px rgba(0,0,0,0.06)`(카드), 강조 시 `0 6px 20px rgba(0,100,255,0.18)`. 여백 — 8pt 그리드.

> 로고 사용 규칙: 회전·색상변경·그라데이션·효과·로고 내 텍스트 삽입 금지. 데모에서는 토스 로고를 직접 쓰지 않고 자체 워드마크/아이콘으로 톤만 차용한다.

---

## 6. 기술 스택 / 구현 계획

| 영역 | 선택 | 근거 |
|------|------|------|
| 프레임워크 | **React + Vite + TypeScript** | 빠른 프로토타이핑, GitHub Pages 배포 용이 |
| 스타일 | Tailwind CSS or CSS Modules | 토스풍 디자인 토큰 관리 |
| 상태 | 로컬 상태(Zustand 또는 React state) | 백엔드 없는 클라이언트 데모 |
| 데이터 | 목업 시드 데이터(6명 페르소나 + 제약 + 회의실) | 즉시 체험 가능한 데모 시나리오 |
| 배포 | GitHub Pages | 제출용 공개 링크 |
| 추천 로직 | 클라이언트 스코어링 함수(`src/lib/recommend.ts`) | 알고리즘 캡슐화·테스트 가능 |

### 6.1 데이터 모델 (핵심 타입 스케치)
```ts
type Availability = 'available' | 'avoid' | 'unavailable';
type UnavailableReason = '외근' | '미출근' | '퇴근후' | '휴가' | '회의' | '기타';
interface Attendee { id; name; role: 'required' | 'optional'; }

// 시간 모델: 30분 단위 블럭. 하루를 09:00~18:00 → 30분 슬롯 인덱스로.
interface Slot { day: number; blockIndex: number; } // blockIndex: 0=09:00-09:30, 1=09:30-10:00 …
// 표시: formatBlock(slot) → "09:00–09:30", formatRange(startSlot, durationMin) → "14:00–15:00"

// 회의 길이 가변 + 후보 기간(캘린더) + 장소 목록
interface Room { id; name; available: SlotKey[]; } // 비어있는(예약 가능) 블럭 집합
interface MeetingConfig {
  title: string;
  durationMinutes: 30 | 60 | 90 | 120;        // 회의 길이(기본 60). 연속 블럭 수 = duration/30
  dateRange: { start: string; end: string };   // ISO 날짜, 캘린더로 선택
  location: 'online' | 'offline';
  rooms: Room[];                                // 오프라인 장소 목록(추가/삭제 가능)
}

interface ConstraintCell {
  attendeeId; slot; status: Availability;       // slot은 30분 블럭
  reason?: UnavailableReason;
  reasonText?: string;
}

// 회의는 연속 blocks(duration/30개)를 점유 → 후보는 "시작 블럭"
interface RankedCandidate {
  startSlot: Slot; blocks: number;              // 점유 블럭 수
  score; satisfied: Attendee[]; yielding: {attendee, reason}[]; absent: Attendee[];
  room?: Room; roomAvailable: boolean; rank: number; // 1~5
}
```

### 6.2 데모 시드 시나리오 (다중 검증)
출제 단서를 반영한 현실적 케이스를 **5종** 탑재한다. 데모 화면 상단에서 시나리오를 전환하며 다양한 충돌 상황을 검증할 수 있다. 각 시나리오는 서로 다른 엣지케이스(완벽한 시간 없음 / 필수자 충돌 / 장소 병목 / 온라인 / 이슈 대응)를 자극하도록 설계. 상세는 **부록 A** 참고.

- 기본값: 시나리오 1(전형적 충돌). 데모는 시나리오 셀렉터로 즉시 전환 가능.
- 데이터 구조: 각 시나리오 = `{ config(장소·기간), attendees(6명·필수/선택), constraints(셀별 3단계 상태) }` JSON 시드.

---

## 7. 작업 분해 (병렬 워크트리 기준)

> 코딩은 opus/sonnet 서브에이전트가 git worktree 격리로 병렬 진행. 영역별 파일 분리. 동일 파일은 한 트랙만.

| 트랙 | 담당(모델) | 범위 | 주요 파일 |
|------|-----------|------|----------|
| A. 스캐폴딩 | opus | Vite+React+TS 셋업, 라우팅, 디자인 토큰 | `vite.config`, `src/main`, `src/styles` |
| B. 타입·알고리즘·시드 | opus | 제약 3단계 모델, 장소, 스코어링(가중치·tie-break·다양성), 1~5순위, **제약완화 제안**, 시드 페르소나 | `src/types.ts`, `src/lib/`, `src/data/` |
| C. 화면 1·2 (생성/제약입력) | sonnet | 온라인·오프라인·필수/선택, 제약 칩 입력(사유 직접입력 포함) | `src/screens/Create`, `Constraints` |
| D. 화면 3·4 (추천/확정) | opus | 1~5순위 카드·trade-off·**반응형 투명성 보드**·드릴다운·제약완화·장소뱃지·URL공유/PNG | `src/screens/Result`, `Confirm` |
| E. 화면 5 (이슈 대응) + 문서·배포 | sonnet | 다음 순위 이동 보드, 3질문 답변, 배포 | `src/screens/Operate`, `SUBMISSION.md`, CI |

*공유 타입(`src/types.ts`)은 트랙 B가 먼저 확정 후 C·D·E 시작.*

---

## 8. 마일스톤

| 단계 | 산출물 | 기한 |
|------|--------|------|
| M1 | PRD 확정 + 스캐폴딩 + 타입/알고리즘 | ~6/30 |
| M2 | 5개 화면 동작 프로토타입 | ~7/5 |
| M3 | 시각 완성도 폴리시 + 데모 시나리오 | ~7/9 |
| M4 | 답변 문서 + 배포 + 최종 점검 | ~7/12 |

---

## 9. 성공 기준 (검증 지표)

- [ ] 6명을 필수/선택으로 구분해 입력할 수 있다 (V1)
- [ ] 참석 상태를 불가·회피·가능 3단계로 입력하고, 불가 사유를 태그(외근·휴가·퇴근후·미출근·회의)로 지정하거나 직접 입력(기타)할 수 있다 (V2)
- [ ] 온라인/오프라인을 선택하고, 오프라인 시 회의실 가용이 후보에 반영된다 (V3)
- [ ] 6명 전원의 가능/회피/불가 상황이 한 화면(투명성 보드)에 명확히 펼쳐진다
- [ ] 특정 시간을 선택하면 **그 시간이 왜 부적합한지(누가 양보·어떤 제약)** 가 드릴다운으로 보인다
- [ ] 추천 결과가 단일 슬롯이 아니라 **1~5순위**로 제시되고 각 순위의 trade-off가 다르다 (V4)
- [ ] "참석자 이슈 발생 → 다음 순위로 이동"이 한 번의 액션으로 동작한다 (V4)
- [ ] 출제 단서(점심직후/외근/필수·선택)가 모두 데모에서 작동·시각화된다
- [ ] 모바일 웹에서 생성→확정→이슈대응까지 끊김 없이 흐른다
- [ ] 3가지 질문 답변이 문제→해결→근거 논리로 연결된다
- [ ] 공개 링크가 외부에서 즉시 열린다(구글드라이브 아님)
- [ ] 부록 A의 5개 시나리오가 모두 데모에서 전환·동작하며 의도한 엣지케이스를 보여준다
- [ ] 후보가 희소/전무할 때 "제약 완화 제안"(풀버전: 4종 완화·gain/cost 정렬·2단계 조합)이 노출되고, 카드 클릭 시 해당 완화를 적용한 결과로 전환·되돌리기된다 (3.5.1)
- [ ] 데모가 빈 화면이 아니라 시드 시나리오가 로드된 상태로 시작해 즉시 가치를 체험할 수 있다

---

## 10. 비범위 및 가정 (데모 스코프 경계)

평가는 *문제 정의·솔루션·시각 완성도*를 보므로, 백엔드성 기능은 의도적으로 데모 범위에서 제외하고 "흐름이 보이는 것"에 집중한다.

**가정**
- 회의 길이 1시간 고정, 기간은 "다음 주" 영업일 5일(월~금), 업무시간 09–18시·점심 12–13시 제외 → 슬롯 40개.
- 참석자는 기본 6명(과제 조건). 추가/삭제는 가능하되 데모 시나리오는 6명 고정.
- 시간대·캘린더 연동 없음. 제약은 시드 데이터 또는 화면 내 수동 입력.

**비범위(Out of scope)** — *의도적 제외, 한계로 명시*
- 실제 계정/로그인, 참석자 응답의 서버 동기화(데모는 주최자 1인 시점 + 1인용 입력 화면 1개로 대체).
- 캘린더(구글/아웃룩) 실연동, 회의실 예약 시스템 실연동(회의실은 시드 가용표로 모사).
- 푸시/이메일 알림·리마인드 발송(확정·공유 카드까지만).
- 동시 편집·실시간 협업.

> 위 비범위는 SUBMISSION의 "왜 이 방식으로 설계했나요"에서 *데모 한계와 제품화 시 확장 방향*으로 정직하게 언급한다.

---

## 부록 A. 데모 시나리오 5종

> 공통 전제(v2): 참석자 6명. **시간은 30분 블럭**(09:00–18:00 → 18블럭/일, 점심 12:00–13:00 제외 시 16블럭). 회의 길이 기본 60분(=연속 2블럭), 기간은 캘린더로 선택한 5영업일. 상태 표기 — ●가능 / ▲회피 / ✕불가(사유). 시드는 30분 블럭 기준으로 재구성한다.
> 각 시나리오는 "검증 목적"이 다르며, 완벽한 슬롯이 없도록 설계해 **양보·순위·투명성**이 의미 있게 드러난다.

### 시나리오 1 — 전형적 충돌 (기본값)
**검증 목적**: 모든 변수(필수/선택, 3단계 상태, 오프라인 장소)가 한 번에 작동하는 표준 케이스.
| # | 이름 | 참석 | 핵심 제약 |
|---|------|------|-----------|
| 1 | 지훈(PM) | 필수 | 화요일 종일 외근 ✕ |
| 2 | 서연(디자인 리드) | 필수 | 매일 점심 직후(13–14시) 회피 ▲, 금요일 오전 집중 ▲ |
| 3 | 민준(개발 리드) | 필수 | 월요일 오전 ✕(미출근), 그 외 ● |
| 4 | 하늘(마케터) | 선택 | 수요일 휴가 ✕ |
| 5 | 도윤(개발) | 선택 | 매일 17시 이후 회피 ▲(퇴근 준비) |
| 6 | 유나(QA) | 선택 | 목요일 오후 외근 ✕ |

**장소**: 오프라인. 회의실 A(종일), 회의실 B(오전만 가용).
**기대**: 필수 3명이 모두 되는 슬롯이 좁고, 점심직후·퇴근전 회피가 겹쳐 완벽한 슬롯 없음 → 1~5순위로 양보 주체가 달라짐. 오프라인이라 오후 슬롯은 회의실 B 불가가 추가 제약.

### 시나리오 2 — 완벽한 시간 없음 (양보 필연)
**검증 목적**: 모든 슬롯에 최소 1명의 회피/불가가 걸려 "양보 없이는 불가능"함을 투명성 보드가 증명.
- 필수 4명 / 선택 2명. 6명의 회피 ▲가 시간대별로 촘촘히 분산되어 **100% 만족 슬롯이 0개**.
- **기대**: 추천 1순위조차 "○○님 1명 회피"를 동반. 드릴다운에서 "이 시간은 도윤님이 양보" 식의 최소 양보 슬롯이 선택됨. 양보 최소화 점수가 잘 작동하는지 검증.

### 시나리오 3 — 필수자끼리 충돌 (후보 희소)
**검증 목적**: 필수 참석자들의 불가 ✕가 서로 어긋나 **가능 후보가 1~2개로 극히 적은** 상황. Hard 제약·필수 가중치 검증.
- 필수 5명 / 선택 1명. 필수자 각자 다른 요일에 외근·휴가 ✕ → 모든 필수자가 동시에 가능한 슬롯이 손에 꼽음.
- **기대**: 후보가 5개 미만이면 순위가 3~4개만 나오고, "더 이상 대안 없음" 안내. 선택자 1명은 자동으로 양보 대상이 되기 쉬움(가중치 낮음). 필수자 충돌 경고 UI 검증.

### 시나리오 4 — 장소 병목 (시간은 OK, 방이 없음)
**검증 목적**: 시간 제약은 느슨하지만 **회의실이 1개뿐**이라 장소가 결정 요인이 되는 V3 강조 케이스.
- 필수 3명 / 선택 3명. 시간 제약은 가벼워 가능 슬롯 다수. 그러나 오프라인 + 회의실 1개(특정 시간대만 비어 있음).
- **기대**: 시간상 좋은 슬롯인데 회의실 ✕로 후보에서 밀리거나 경고 뱃지. 드릴다운에서 "시간은 모두 가능하나 회의실 없음"이 부적합 사유로 노출. 장소 변수의 가치 입증.

### 시나리오 5 — 이슈 발생 → 다음 순위 (V4 운영 데모)
**검증 목적**: 확정 후 참석자 이슈로 **1순위가 무너졌을 때 다음 순위 이동**이 매끄럽게 동작.
- 시나리오 1을 확정한 상태에서 시작. 1순위 시간에 "지훈(필수)님 긴급 이슈로 불참" 이벤트.
- **기대**: 운영 화면에서 [다음 순위로 이동] 1클릭 → 2순위로 승격, 변경 이력 기록, 재공유 카드 갱신. 재조율 없이 의사결정이 끝나는 경험 검증.

> 구현 메모: 5개 시나리오는 `src/data/scenarios.ts`에 배열로 시드. 시나리오 2·3은 알고리즘의 "양보 최소화"와 "후보 희소 처리"를, 4는 장소 가중치를, 5는 운영 상태 전이를 각각 집중 검증한다.
